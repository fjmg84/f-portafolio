---
const { name, bgColor, color } = Astro.props;
---

<span
  class="px-4 py-2 text-sm rounded-full text-nowrap w-fit font-extralight"
  style={`background-color: ${bgColor || "#333"}; color: ${color || "white"};`}
>
  {name}
</span>
